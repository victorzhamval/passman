import { createRef } from 'react';

const loadVaults = createRef(() => { });
const loadProfile = createRef(() => { });
const navigate = createRef(() => { });

export {
  loadVaults,
  loadProfile,
  navigate
}
